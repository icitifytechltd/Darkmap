#!/bin/bash
set -e

# Create symlink only if the executable exists
if [ -f "/usr/local/lib/darkmap/darkmap.py" ]; then
    ln -sf /usr/local/lib/darkmap/darkmap.py /usr/local/bin/darkmap
    chmod +x /usr/local/bin/darkmap
fi

exit 0
